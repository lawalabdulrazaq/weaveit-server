# Database Configuration
# PostgreSQL connection string (required for Heroku)
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://user:password@localhost:5432/weaveit_db

# OpenAI Configuration (required for text-to-speech and code analysis)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Server Port (optional, Heroku will set this automatically)
# Leave unset in production; Heroku assigns dynamically
# For local dev: PORT=3001
PORT=3001

# Node Environment
NODE_ENV=production

# Database SSL Mode (optional, for production databases)
# PGSSLMODE=require
